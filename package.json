{
  "name": "wechat-auto-web",
  "version": "0.1.0",
  "private": true,
  "description": "本地 web 端：读取 md 设计文档，调 AI 生成 skill 与微信公众号格式 html",
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm -C server run dev\" \"npm -C web run dev\"",
    "dev:server": "npm -C server run dev",
    "dev:web": "npm -C web run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
